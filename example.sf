dfa automataExample { // dfa
  initial q0;
  state q1;
  final q2;

  on q0->q1 when "1";
  //on q0 -> q2 when "1";
  on q1->q2 when "2";
  //on q2 -> q2 when "3" or /\d/;
}

fn main(input) {
  automataExample <- input;
}
